<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mp4.com.mp4" >

    <application
        android:name=".applications.AppContainer"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Options"
            android:label="@string/title_activity_options" >
        </activity>
        <activity
            android:name=".activities.RoomSearch"
            android:label="@string/title_activity_room_search" >
        </activity>
        <activity
            android:name=".activities.PlaceSearch"
            android:label="@string/title_activity_place_search" >
        </activity>
        <activity
            android:name=".activities.Map"
            android:label="@string/title_activity_map" >
        </activity>
        <activity
            android:name=".RoomDescription"
            android:label="@string/title_activity_room_description" >
        </activity>
    </application>

</manifest>
